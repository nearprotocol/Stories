# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Stories' do
  use_frameworks!

  # Pods for Stories
  pod "SwiftyCam", :git => 'git@github.com:nearprotocol/SwiftyCam.git'
end
